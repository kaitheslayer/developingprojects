def word_count(string):
    string =  " " + string
    words = 0
    y = 0
    while y <= len(string):
        if string[y] != " " and string[y+1] == " ":
            words += 1
        y += 1
    return words

print(word_count("stuff stuff stuff"))

# the 'for loop' had a problem, maybe a 'while loop' will work. Nope, i give up
